import { StyleSheet, Text, View } from "react-native";
import React from "react";
import Memberships from "../components/Auth/Memberships";

const membership = () => {
  return (
    <View>
      <Memberships />
    </View>
  );
};

export default membership;

const styles = StyleSheet.create({});
